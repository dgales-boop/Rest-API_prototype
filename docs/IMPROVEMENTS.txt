IMPROVEMENTS — For next versions

High (v2)
- JWT (optional) + keep API keys.
- Webhook signing (HMAC).
- Webhook retry + backoff.
- GET /api/v1/protocol-executions/:id.
- Rate limiting (e.g. per key).
- Request/response logging (audit).

Medium (v2/v3)
- OpenAPI/Swagger.
- Scopes per API key.
- Webhook URL per tenant (in DB).
- DB migrations (no inline CREATE TABLE).
- Automated tests (e.g. Jest + supertest).

Low (v3+)
- Pagination for list endpoint.
- GET /health.
- Simulate "rejected" status.
- Dockerfile for API (full stack in compose).
- MCP layer for AI agents.
