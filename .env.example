# Server
PORT=4001

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=proto_user
DB_PASSWORD=proto_secret
DB_NAME=proto_db

# API Keys → Tenant mapping (JSON)
# Format: { "api-key-value": "tenant-id" }
API_KEYS={"rh_test_alpha_92f4c1":"tenant-alpha","rh_test_beta_7d3e8a":"tenant-beta"}

# Webhook signing secret (used for HMAC-SHA256 signature in X-Webhook-Signature header)
WEBHOOK_SECRET=whsec_proto_a1b2c3d4e5f6
